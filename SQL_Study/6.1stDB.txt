CREATE First DataBase :-

	CREATE DATABASE db_name;

	
	DROP DATABASE db_name;	#Delete DataBase

Creating First Table :-

	USE db_name;

	CREATE TABLE table_name(					CREATE TABLE student (
		column_name1 datatype constraint,  \			     id INT PRIMARY KEY,datatype = INT constraint = PRIMARY KEY
		column_name2 datatype constraint,   \		             name VARCHAR(50),
		column_name3 datatype constraint,   /	SCHEMA / Design	     age INT NOT NULL
			);			   /	    DEFINE		     );

										student
										+----+------+-----+
										| id | name | age |
										+----+------+-----+
										|    |	    |	  |
										|    |      |     |

									INSERT INTO student VALUES( 1, "SURAJ", 22);
									INSERT INTO student VALUES( 2, "HARSH", 19);

											SELECT * FROM student;